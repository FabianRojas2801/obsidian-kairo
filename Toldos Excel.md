- **Lona:** Se redondea a 2 decimales la siguiente fórmula: `(Linea / 100) * [(Salida + 60) / 100]`
$$
\frac{\text{Linea}}{100}
\cdot
\frac{\text{Salida} + {60}}{100}
$$

- **Confección:** Busca en la tabla de Confección (de `AC:4` a `AN:18`) el valor que corresponde a la intersección entre la fila **Salida** y la columna **Línea**, con coincidencia exacta en ambos casos.
- 